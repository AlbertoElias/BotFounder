<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Send message</title>
  </head>
  <body>
    <h4>{{.ok}}</h4>
    <form class="" action="{{.postTo}}" method="post">
      <input type="text" name="message"/>
      <input type="submit" value="Send"/>
    </form>
  </body>
</html>
